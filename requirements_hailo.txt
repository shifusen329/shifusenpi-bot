# Hailo Integration Requirements

# Hailo platform (installed via Hailo setup)
# hailo-platform>=4.18.0  # Installed separately

# GStreamer Python bindings
# Note: Install via apt instead: sudo apt install python3-gst-1.0

# Spatial reasoning and math
scikit-learn>=1.3.0
scipy>=1.11.0

# Performance and threading
multiprocessing-logging>=0.3.4

# Data structures
dataclasses-json>=0.6.0

# Include base requirements
-r requirements.txt
